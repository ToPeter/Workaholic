<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="25dp" >
    
     <TextView
         android:id="@+id/existing_account_text"
         android:layout_width="match_parent"
         android:layout_height="wrap_content"
         android:layout_alignParentTop="true"
         android:gravity="center"
         android:textSize="16sp"
         android:textColor="@android:color/white"
         android:layout_marginBottom="10dp"
         android:text="There is already an existing account, in order to register again, enter the password to delete it!"/>
     
     <EditText 
         android:id="@+id/password_to_delete"
         style="@style/LoginActivityField.Password.Normal"
         android:layout_below="@id/existing_account_text"
         android:layout_marginBottom="10dp"
         android:textColor="@android:color/black"/>
     	 
     <LinearLayout 
         android:id="@+id/button_panel_recover"
         android:layout_below="@id/password_to_delete"
         android:layout_width="match_parent"
         android:layout_height="wrap_content"
         android:orientation="horizontal">
         
         <Button 
             android:id="@+id/cancel"
             android:layout_width="0dp"
             android:layout_height="match_parent"
             android:layout_weight="1"
             android:text="Cancel"
             android:textColor="@android:color/white"
             android:background="@drawable/green_btn_default_holo_dark"/>
         
         <Button
	         android:id="@+id/register_new"
	         android:layout_width="0dp"
	         android:layout_height="match_parent"
	         android:layout_weight="1"
	         android:text="Delete Old Account"
	         android:textColor="@android:color/white"
	         android:background="@drawable/red_btn_default_holo_dark" />
         
     </LinearLayout>
 
</RelativeLayout>

