<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="start"
    android:background="@color/dark_workaholic"
    tools:context=".NavigationDrawerFragment">

    <!-- Listview to display slider menu -->
    <ListView
        android:id="@+id/list_slidermenu"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/logout"
        android:choiceMode="singleChoice"
        android:divider="@color/highlight_workaholic"
        android:dividerHeight="1dp"
        android:listSelector="@drawable/workaholic_list_selector_holo_light"
        android:background="@color/dark_workaholic"/>

    <Button
        android:id="@+id/logout"
        android:layout_width="match_parent"
        android:layout_height="68dp"
        android:layout_alignParentBottom="true"
        android:background="@drawable/red_btn_default_holo_dark"
        android:text="@string/logout"
        android:textColor="@android:color/white"
        android:drawableLeft="@drawable/front_desk"/>

</RelativeLayout>



